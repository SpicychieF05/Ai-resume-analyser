# Example secrets file for Streamlit deployment
# Copy this to secrets.toml and add your actual values
# Note: secrets.toml is gitignored for security

# Example API keys (not currently used but ready for future features)
# openai_api_key = "your-openai-api-key-here"
# google_api_key = "your-google-api-key-here"

# Database configuration (for future user accounts feature)
# db_username = "your-db-username"
# db_password = "your-db-password"
# db_url = "your-database-url"

# Analytics (for future tracking features)
# google_analytics_id = "your-ga-id"

# Email service (for future notification features)
# smtp_server = "smtp.gmail.com"
# smtp_port = 587
# email_username = "your-email@gmail.com"
# email_password = "your-app-password"
